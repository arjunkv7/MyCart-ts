import models from '../models/index'
import bcrypt from 'bcrypt';
import jwt from 'jsonwebtoken'

export let userJwtAuth = (req,res, next) => {
    return new Promise((resolve, reject) => {
        try {
            
        } catch (error) {
            console.log(error);
            
        }
    });
} 